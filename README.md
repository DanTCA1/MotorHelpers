# Currently in development

### Utility file to add functions to motors in FRC's WPILIB such as:
- Loggability through SmartDashboard's sendable class, including elastic support
- Ability to dynamically change PID constants
- Ability to read what values are put in motor configs
